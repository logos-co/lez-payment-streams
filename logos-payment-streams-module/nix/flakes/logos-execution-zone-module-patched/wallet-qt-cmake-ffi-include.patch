diff --git a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -181,6 +181,10 @@ qt_add_plugin(${PLUGIN_TARGET} CLASS_NAME LogosExecutionZoneWalletModule)
 target_include_directories(${PLUGIN_TARGET} PRIVATE
   ${CMAKE_CURRENT_SOURCE_DIR}/src
 )
+
+# wallet_ffi.h must be on the compile line for prebuilt wallet-ffi; INTERFACE_INCLUDE_DIRECTORIES
+# on the IMPORTED target is not always propagated to qt_add_plugin targets (Qt 6.9 + Ninja).
+target_include_directories(${PLUGIN_TARGET} PRIVATE "${LOGOS_EXECUTION_ZONE_WALLET_INCLUDE}")
 
 target_link_libraries(${PLUGIN_TARGET} PRIVATE
   Qt6::Core
