[package]
name = "lez-payment-streams-core"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[features]
default = ["host"]
# Host-side LEE (`lee` crate): instruction wire + off-chain proofs. Disabled for RISC0 guest builds.
host = ["dep:lee", "nssa_core/host", "dep:sha2", "dep:programs", "dep:authenticated_transfer_core"]
# Privacy-preserving LEE proving tests in `program_tests` (slow). Enable with `--features pp-program-tests`.
# `nssa_core/test_utils` exposes encapsulate_deterministic for deterministic PP harness keys.
pp-program-tests = ["nssa_core/test_utils"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
borsh = "1.5"
hex = "0.4"
risc0-zkvm = { version = "=3.0.5", features = ["std"] }
# LEZ harness pin: v0.2.0 (Step 26) — in-process program_tests.
# Operational scaffold / wallet / testnet pin is v0.2.0; see N16.
lee = { git = "https://github.com/logos-blockchain/logos-execution-zone.git", tag = "v0.2.0", features = ["test-utils"], optional = true }
nssa_core = { package = "lee_core", git = "https://github.com/logos-blockchain/logos-execution-zone.git", tag = "v0.2.0", default-features = false }
clock_core = { git = "https://github.com/logos-blockchain/logos-execution-zone.git", tag = "v0.2.0" }
programs = { git = "https://github.com/logos-blockchain/logos-execution-zone.git", tag = "v0.2.0", default-features = false, features = ["artifacts"], optional = true }
authenticated_transfer_core = { git = "https://github.com/logos-blockchain/logos-execution-zone.git", tag = "v0.2.0", optional = true }
sha2 = { version = "0.10", optional = true }
# Stock spel v0.6.0. Macros expand to nssa_core:: — see extern crate alias in lib.rs.
spel-framework-macros = { git = "https://github.com/logos-co/spel.git", tag = "v0.6.0", package = "spel-framework-macros" }
spel-framework-core = { git = "https://github.com/logos-co/spel.git", tag = "v0.6.0", package = "spel-framework-core" }

[[bin]]
name = "n8_canonical_wire_hex"
path = "src/bin/n8_canonical_wire_hex.rs"
required-features = ["host"]
